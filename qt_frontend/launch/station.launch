<?xml version="1.0"?>
<launch>
  <!--
    Robot link static transforms are forwarded through /tf_static by the
    ROS Agent and MQTT-ROS Bridge. Fleet root frames are configured in
    bridge/bridge_config.yaml and published by the Bridge.
  -->
</launch>
